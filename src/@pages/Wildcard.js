import * as C from "@components";

export const Wildcard = () => {
  return (
    <C.Page>
      <h1>Wildcard page</h1>
      <p>
        This is the Wildcard page. It is rendered when no other page is found.
      </p>
    </C.Page>
  );
};

export default Wildcard;
